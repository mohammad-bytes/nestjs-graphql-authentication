{
    "version": 2,
    "env": {
        "PORT": "@PORT",
        "JWT_SECRET": "@JWT_SECRET",
        "DB_URL": "@DB_URL",
        "DB_NAME": "@DB_NAME"
    },
    "builds": [
        {
            "src": "src/main.ts",
            "use": "@vercel@node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/main.ts",
            "methods": [
                "POST",
                "GET",
                "POST",
                "PATCH",
                "DELETE"
            ]
        }
    ]
}